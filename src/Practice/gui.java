package Practice;
import javax.swing.JOptionPane;
public class gui {
    public static void main(String[] args) {
 /*
        String name = JOptionPane.showInputDialog("enter your name");
        JOptionPane.showInputDialog(null, "hello"+name);
int age = Integer.parseInt(JOptionPane.showInputDialog("enter your age"));
JOptionPane.showInputDialog(null, "you are"+age+"years old");

**/





 int a=3;
 int b = 4;
 int c =1;
         int z = (a*b+c); //this are two different calculations, this one gives a * b + c

         int h =  (a*(b+c));// this gives b + c * a=15 because the algebra expression
        // in the nested bracket execute first before the one in th
        //major parentcesse
        System.out.println(z);
    }
}
